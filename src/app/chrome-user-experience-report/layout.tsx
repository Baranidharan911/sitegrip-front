import AppContent from '@/components/Layout/AppContent';

export default function ChromeUserExperienceReportLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <AppContent>{children}</AppContent>;
} 