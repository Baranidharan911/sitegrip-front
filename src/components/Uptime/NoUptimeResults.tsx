import React from 'react';

export default function NoUptimeResults() {
  return (
    <div className="text-center text-gray-500 dark:text-gray-400 py-12">
      <p>No URLs monitored yet. Add one above to begin monitoring uptime.</p>
    </div>
  );
}